rootProject.ext.setApplication(project.name)

apply from: "${rootProject.rootDir}/universal.gradle"

dependencies {
    implementation project(path:':Baselibs')
    implementation project(path:':login')
    implementation project(path:':member')
}
